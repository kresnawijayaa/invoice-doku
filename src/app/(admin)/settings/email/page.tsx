export default function EmailSettingsPage() {
  return (
    <main className="px-4 py-5 sm:p-6">
      <section className="mx-auto max-w-4xl rounded-lg border border-line bg-panel p-4 text-sm text-muted shadow-sm sm:p-6">
        Pengaturan email akan dibuat pada tahap integrasi email.
      </section>
    </main>
  );
}
